{% extends 'frontbase.html.twig' %}

{% block title %}
    Welcome Back
    {{app.user.firstName ~ ' ' ~ app.user.lastName}}

{% endblock %}
{% block body %}
    <h1>Edit Regime</h1>

    {{ include('regime/_form.html.twig', {'button_label': 'Update'}) }}

    <button  type="button" class="btn btn-primary"><a style="color: white" href="{{ path('regime_index') }}">back to list</a> </button>

    {{ include('regime/_delete_form.html.twig') }}
{% endblock %}
