{{ form_start(form) }}
     {{ form_row(form.code, {'attr': {'class': 'form-control form-control-user'}}) }}
 {{ form_row(form.mouvement, {'attr': {'class': 'form-control form-control-user'}}) }}
 {{ form_row(form.description, {'attr': {'class': 'form-control form-control-user'}}) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
