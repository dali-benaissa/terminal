{% extends 'frontbase.html.twig' %}

{% block title %}
    Welcome Back
    {{app.user.firstName ~ ' ' ~ app.user.lastName}}

{% endblock %}

{% block body %}
    <h1>Exercice</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Code</th>
                <td>{{ exercice.code }}</td>
            </tr>
            <tr>
                <th>Mouvement</th>
                <td>{{ exercice.mouvement }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ exercice.description }}</td>
            </tr>
        </tbody>
    </table>

    <button  type="button" class="btn btn-primary"><a style="color: white" href="{{ path('exercice_index') }}">back to list</a> </button>

    <button  type="button" class="btn btn-secondary"> <a style="color: white" href="{{ path('exercice_edit', {'id': exercice.id}) }}">edit</a></button>

    {{ include('exercice/_delete_form.html.twig') }}
{% endblock %}
